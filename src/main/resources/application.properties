server.port=8743
#server.servlet.context-path=/api/v1.0/
spring.profiles.active=dev
spring.application.name=stock-price-service

#Spring 2.6.0 above has the probelm when using spring fox, either use Spring Doc or add the following param to resolve the issue
#the spring boot application fails to start without this param
spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER

